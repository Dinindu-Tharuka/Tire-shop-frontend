import React from 'react'
import { useForm } from 'react-hook-form'

const BillAddPayment = () => {
  const { register, handleSubmit} = useForm();
    return (
    <div>
      <form>

      </form>
    </div>
  )
}

export default BillAddPayment